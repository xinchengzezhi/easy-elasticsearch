# spring config
spring.profiles.active=dev
spring.application.name=easy-elasticsearch
spring.arthas.enabled=false
apollo.bootstrap.namespaces=monitor
server.port=8080
#
logging.file.path=./liangbaole/logs/${spring.application.name}

